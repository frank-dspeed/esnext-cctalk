// Uses the payloadHelper isMethods.
/**
 * replacement for the on complet parser
 * emits CCTalk Tokens as 8 byte Arrays
 * @param {*} payload 
 */
const tokenizer = payload => {
    
    // check if message contains at last one complet token
    // find first matching token in message
}
export {}